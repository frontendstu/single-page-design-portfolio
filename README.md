# Sass Primer

Sass Primer is a Sass template that you can use to kickstart any Sass-based project. It includes a set of common Sass mixins, functions, and variables that you can use to style your web pages quickly and efficiently.

### Getting Started
To use Sass Primer start by running `npm install`, then once the packages are installed, simply run `npm start`
